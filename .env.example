# URL base do Bill (sem barra final)
BILL_API_URL=http://localhost:3003

# JWT com claim email ou sub (mesmo segredo que o Bill — ver repo bidnoculus_bill)
# Gerar/atualizar a partir do Bill: npm run token:atualizar-agent-env
BILL_API_TOKEN=
